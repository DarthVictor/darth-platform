enum DataTypes {
    ATTACHMENT,
    STRING,
    TEXT,
    DATE,
    DATETIME,
    TIME,
    INTEGER,
    FLOAT,
    BOOLEAN,
    LABEL,
    VIRTUAL,
    DICTIONARY,
    COMPLEX
}
;

enum Scalarity {
    SCALAR,
    VECTOR
}
;

enum TextPatterns {
    SIMPLE,
    CODE,
    RICHTEXT
}
;

enum StringPatterns {
    SIMPLE,
    PASSWORD,
    EMAIL,
    URL,
    PHONE
}
;

enum DictionaryPatterns {
    ADDABLE_AUTOMPLETE,
    ADDABLE_COMBOBOX,
    AUTOMPLETE,
    COMBOBOX,
}
;

enum VirtualStyles {
    GRID_TABLE,
    SIMPLE_TABLE,
    TREE_TABLE,
    PIVOT_TABLE,
    SCHEDULER,
    SCHEDULER_TIMELINE,
    BAR_CHART,
    LINE_CHART,
    PIE_CHART
}

